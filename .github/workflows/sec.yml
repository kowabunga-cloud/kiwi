name: Security Checks

on:
  push:
    branches: master
  pull_request:
    branhces: master
  workflow_dispatch:

jobs:
  sec:
    uses: kowabunga-cloud/workflows/.github/workflows/go-sec.yml@master
    secrets: inherit
