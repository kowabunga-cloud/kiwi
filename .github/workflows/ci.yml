name: Continuous Integration test

on:
  push:
    branches: master
  pull_request:
    branhces: master
  workflow_dispatch:

jobs:
  ci:
    uses: kowabunga-cloud/workflows/.github/workflows/go-ci.yml@master
    secrets: inherit
